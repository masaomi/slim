<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @category.name %>
</p>
<p>
  <strong>Lipids:</strong>
  <%= @category.lipids.length %><br \>
  <%= @category.lipids.map{|lip| lip.id}.to_s %>
</p>

<%= link_to 'Edit', edit_category_path(@category) %> |
<%= link_to 'Back', categories_path %>
